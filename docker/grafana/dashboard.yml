apiVersion: 1

providers:
  - name: 'impeller-dashboard'
    orgId: 1
    # <string> name of the dashboard folder.
    folder: ''
    type: file
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/dashboards
      foldersFromFilesStructure: true